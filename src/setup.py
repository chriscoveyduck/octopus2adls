from setuptools import setup, find_packages

setup(
    name="octopus2adls_shared",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    description="Shared clients and utilities for octopus2adls Azure Functions.",
    author="chriscoveyduck",
    author_email="",
    include_package_data=True,
)
